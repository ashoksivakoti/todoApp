import { DeleteForeverFontIcon } from "@react-md/material-icons";
import {RiDeleteBin2Fill} from "react-icons/ri"

const Note = ({ id, text, date, handleDeleteNote }) => {
	return (
		<div className='note'>
			<span>{text}</span>
			<div className='note-footer'>
				<small>{date}</small>
				<RiDeleteBin2Fill
					onClick={() => handleDeleteNote(id)}
					className='delete-icon'
					size='1.3em'
				/>
			</div>
		</div>
	);
};

export default Note;
